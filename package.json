{
	"name": "lil2",
	"version": "0.9.0",
	"license": "MIT",
	"private": false,
	"main": "dist/LinkedList.js",
	"type": "module",
	"files": [
		"dist"
	],
	"scripts": {
		"test": "vitest run",
		"build": "if [ -d dist ]; then rm -rf dist; fi && tsc --outDir dist",
		"prepack": "npm run build",
		"format": "biome format --write",
		"perf": "if [ -d perf/dist ]; then rm -rf perf/dist; fi && tsc --outDir perf/dist && node perf/fixImports.js && node perf/list.js",
    "perf:iterator": "if [ -d perf/dist ]; then rm -rf perf/dist; fi && tsc --outDir perf/dist && node perf/fixImports.js && node perf/iterator.js"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.10",
		"@types/lodash": "^4.17.7",
		"benchmark": "^2.1.4",
		"lodash": "^4.17.21",
		"microtime": "^3.1.1",
		"typescript": "^5.5.4",
		"vitest": "^3.2.4"
	}
}
